dependencies {
    compile project(":piper:common")
    compile project(":piper:exception-mapping")
    compile libs.ktor_jackson
}

allprojects {
    if (!project.path.matches(':piper:util')) {
        dependencies {
            compile project(":piper:util")
        }
    }
}
