import React from 'react';
import styles from './SectionSpacer.module.scss';

/**
 * A significant gap within a [Section].
 */
const SectionSpacer: React.FC = () => {
  return <div className={styles.spacer} />;
};

export default SectionSpacer;
