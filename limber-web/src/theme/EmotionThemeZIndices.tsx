/**
 * Feel free to change these at will if there's "no room" between two values. Stability between
 * releases is not a concern.
 */
export const EmotionThemeZIndices = {
  default: 0,
  topLevelNavbarSubnav: 1,
};
