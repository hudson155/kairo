This directory contains only simple hooks for use in the app.
There are more elaborate hooks such as `useApi()` and `useAuth()` elsewhere,
where they are important enough to have their own separate directory.
