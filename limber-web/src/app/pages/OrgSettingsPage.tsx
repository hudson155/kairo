import React from 'react';

export const orgSettingsPagePath = () => '/org-settings';

/**
 * Root page for managing org settings.
 */
const OrgSettingsPage: React.FC = () => {
  return <p>Org Settings</p>;
};

export default OrgSettingsPage;
