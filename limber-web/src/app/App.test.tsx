import { render } from '@testing-library/react';
import React from 'react';

import App from './App';

/**
 * TODO: This is a placeholder test. We should write significantly more tests.
 */
test('renders without throwing an error', () => {
  render(<App />);
});
