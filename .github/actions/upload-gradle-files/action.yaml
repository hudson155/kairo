name: Upload Gradle files
description: Uploads all Server JAR files.
runs:
  using: composite
  steps:
    - uses: actions/upload-artifact@v3.1.0 # https://github.com/actions/upload-artifact/releases.
      with:
        name: gradle-files
        path: server/*/build/libs/server.jar
        retention-days: 1
