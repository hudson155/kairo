sqlDatabase:
  jdbcUrl: jdbc:postgresql:///limber
  username: limber
  password:
    type: PLAINTEXT
    value: H731t4zJsoRUKE$7%vS$D9WA
  properties:
    # https://cloud.google.com/sql/docs/mysql/connect-app-engine
    socketFactory: com.google.cloud.sql.postgres.SocketFactory
    cloudSqlInstance: limberapp:us-central1:limberapp-prod
    useSSL: false

authentication:
  mechanisms:
    - type: JWK
      issuer: https://limber.auth0.com/
      domain: limber.auth0.com

serving:
  apiPathPrefix: /api
  staticFiles:
    serve: true
    rootPath: .
