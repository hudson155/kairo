mongoDatabase:
  protocol: MONGODB
  host: localhost
  database: limberapp

authentication:
  mechanisms:
    - type: JWT
      issuer: https://limberapp.io/
      secret: limberapp

serving:
  apiPathPrefix: /
  staticFiles:
    serve: false
