package io.limberapp.config

/**
 * Implementations should be string-string key-value pairs, where each value is the host for another
 * server that the server needs to interact with.
 */
interface Hosts
