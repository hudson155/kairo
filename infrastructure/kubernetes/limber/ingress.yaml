apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: limber-ingress
  namespace: limber
  labels:
    instance: limber
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - limberapp.io
        - api.limberapp.io
      secretName: limber-tls
  rules:
    - host: limberapp.io
      http:
        paths:
          - backend:
              serviceName: limber-web
              servicePort: 80
    - host: api.limberapp.io
      http:
        paths:
          - backend:
              serviceName: limber-backend
              servicePort: 80
